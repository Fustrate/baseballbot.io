.row
  .small-12.columns
    %h4 Today's Game Threads

    %table
      %thead
        %tr
          %th GID
          %th Title
          %th Subreddit
          %th Post At
          %th Starts At
          %th Status
      %tbody
        - @gamechats.each do |gamechat|
          %tr
            %td= gamechat.gid
            %td= link_to gamechat.title, "http://redd.it/#{gamechat.post_id}" if gamechat.post_id.present?
            %td= link_to gamechat.subreddit.name, "http://reddit.com/r/#{gamechat.subreddit.name}"
            %td= gamechat.post_at.strftime '%-I:%M %p'
            %td= gamechat.starts_at.strftime '%-I:%M %p'
            %td= gamechat.status
