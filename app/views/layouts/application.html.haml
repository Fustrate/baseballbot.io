!!!
%head
  %title baseballbot.io
  = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
  = javascript_include_tag 'application', 'data-turbolinks-track' => true
  = csrf_meta_tags
%body
  - unless flash.empty?
    - flash.each do |type, message|
      %div.alert-box{ class: type }
        %a.close{ href: '#' } &times;
        = message
    - flash.clear

  = yield
